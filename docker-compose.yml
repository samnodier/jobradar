services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=sam
      - POSTGRES_PASSWORD=password123
      - POSTGRES_DB=jobradar
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
